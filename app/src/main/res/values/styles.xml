<resources>

    <!-- Base application theme. -->
    <style name="ChatAppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorAccent</item>
        <item name="colorPrimaryDark">@color/colorAccent</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:typeface">serif</item>
    </style>



    <style name="SecondActivityTheme" mce_bogus="1" parent="ChatAppTheme">
        <item name="android:windowNoTitle">true</item>
        <item name="android:launchMode">singleTask</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>



    <!--dialog-->
    <style name="MyDialog" parent="android:Theme.Dialog">
        <!-- 背景颜色及透明程度 -->
        <item name="android:windowBackground">@color/colorDialog</item>
        <!-- 是否半透明 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- 是否没有标题 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否背景模糊 -->
        <item name="android:backgroundDimEnabled">true</item>

    </style>



    <style name="Boxing.ToolbarTheme" parent="Boxing.AppTheme.NoActionBar" >
        <item name="titleTextColor">@color/colorWhite</item>
        <item name="android:textColorSecondary">@color/colorWhite</item>
        <item name="android:textColor">@color/colorWhite</item>
        <item name="android:background">@color/colorBlack</item>
    </style>
    <!-- BoxingActivity Theme-->
    <style name="Boxing.AppTheme.NoActionBar" parent="ChatAppTheme">
        <item name="android:windowNoTitle">true</item>
        <item name="android:fitsSystemWindows">false</item>
    </style>
    <!-- TextAppearance -->
    <style name="Boxing.TextAppearance.App.Medium" parent="TextAppearance.AppCompat">
        <item name="android:textSize">12sp</item>
    </style>
    <style name="Boxing.PopupAnimation" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/boxing_fade_in</item>
        <item name="android:windowExitAnimation">@anim/boxing_fade_out</item>
    </style>

    <declare-styleable name="CircleRelativeLayoutLayout">
        <attr name="background_color" format="color" />
        <attr name="background_alpha" format="integer" />
    </declare-styleable>

</resources>
